# 电影管理平台

> 一个基于 Vue 3 + Express + MySQL 的前后端分离电影管理平台

## 项目简介

本项目是一个完整的前后端分离的电影管理系统，包含用户端和管理后台。用户可以浏览电影、收藏、评论和发布帖子；管理员可以管理电影、分类、用户、公告等数据。

**架构特点：**

- ✅ **前后端分离**：前端 Vue 3（端口 5173），后端 Express（端口 3000）
- ✅ **RESTful API**：标准化的 API 接口设计
- ✅ **响应式设计**：适配移动端和桌面端
- ✅ **实时数据同步**：所有 CRUD 操作立即更新页面

## 技术栈

### 前端

- **框架：** Vue 3 (Composition API)
- **构建工具：** Vite 5
- **UI 组件库：** Element Plus + Ant Design Vue
- **状态管理：** Pinia
- **路由管理：** Vue Router
- **HTTP 客户端：** Axios
- **富文本编辑器：** Quill
- **图表库：** ECharts
- **样式：** SCSS

### 后端

- **运行环境：** Node.js
- **Web 框架：** Express 5
- **数据库：** MySQL 8.0
- **数据库客户端：** mysql2
- **跨域处理：** CORS
- **模块系统：** CommonJS (.cjs)

## 项目结构

```
dianying/
├── src/                          # 前端源码
│   ├── api/                      # API 接口封装
│   │   └── index.js             # 统一 API 管理
│   ├── components/              # 组件目录
│   │   ├── backend/             # 后台管理组件
│   │   │   ├── pages/           # 管理页面
│   │   │   │   ├── Index.vue           # 后台首页（统计数据）
│   │   │   │   ├── Category.vue        # 分类管理
│   │   │   │   ├── Goods.vue           # 电影管理
│   │   │   │   ├── Users.vue           # 用户管理
│   │   │   │   ├── Announcements.vue   # 公告管理
│   │   │   │   └── Setting.vue         # 系统设置
│   │   │   ├── Layout/          # 后台布局组件
│   │   │   │   ├── Aside.vue           # 侧边栏导航
│   │   │   │   ├── Header.vue          # 顶部导航栏
│   │   │   │   └── GoodsEdit.vue       # 电影编辑组件
│   │   │   ├── Index.vue        # 后台入口
│   │   │   └── Login.vue        # 后台登录
│   │   ├── subcomponents/       # 前台子组件
│   │   │   ├── HomePage.vue            # 前台首页
│   │   │   ├── Movies.vue              # 电影列表
│   │   │   ├── Communication.vue       # 论坛交流
│   │   │   ├── DataBase.vue            # 电影数据库
│   │   │   ├── UserCenter.vue          # 个人中心
│   │   │   ├── qianlogin.vue           # 前台登录
│   │   │   ├── register.vue            # 用户注册
│   │   │   └── Layout/                 # 前台布局
│   │   │       ├── MyHeader.vue        # 前台头部
│   │   │       └── MyFooter.vue        # 前台底部
│   │   ├── user/                # 用户相关组件
│   │   │   └── MyUserDetail.vue        # 用户详情
│   │   └── Home.vue             # 前台主页面
│   ├── stores/                  # Pinia 状态管理
│   │   ├── auth.js              # 认证状态
│   │   ├── user.js              # 用户信息
│   │   ├── users.js             # 用户列表
│   │   ├── category.js          # 分类管理
│   │   ├── movieStore.js        # 电影数据
│   │   ├── favorites.js         # 收藏功能
│   │   └── token.js             # Token 管理
│   ├── router/                  # 路由配置
│   │   └── router.js            # 路由定义
│   ├── utils/                   # 工具函数
│   │   └── operationLog.js      # 操作日志（localStorage）
│   ├── directive/               # 自定义指令
│   │   └── index.js
│   ├── assets/                  # 静态资源
│   │   └── img/                 # 图片资源
│   ├── App.vue                  # 根组件
│   ├── main.js                  # 入口文件
│   └── style.css                # 全局样式
├── server/                      # 后端服务器
│   ├── routes/                  # API 路由
│   │   ├── movies-mysql.cjs            # 电影相关 API
│   │   ├── categories-mysql.cjs        # 分类 API
│   │   ├── users-mysql.cjs             # 用户 API
│   │   ├── announcements-mysql.cjs     # 公告 API
│   │   ├── posts-mysql.cjs             # 帖子 API
│   │   ├── hot-movies-mysql.cjs        # 热门电影 API
│   │   ├── rankings-mysql.cjs          # 排行榜 API
│   │   ├── carousel-mysql.cjs          # 轮播图 API
│   │   └── admininfo-mysql.cjs         # 管理员信息 API
│   ├── db-mysql.cjs             # MySQL 连接配置
│   ├── index-mysql.cjs          # 服务器入口
│   ├── init-mysql.cjs           # 数据库初始化脚本
│   └── clear-db.cjs             # 数据库清空脚本
├── public/                      # 公共静态资源
├── index.html                   # HTML 入口
├── vite.config.js               # Vite 配置
├── package.json                 # 项目依赖
└── README.md                    # 项目文档
```

## 功能模块

### 用户端功能

- 🎬 **电影浏览**：电影列表、详情、搜索、分类筛选
- ⭐ **收藏管理**：添加/取消收藏、查看收藏列表
- 💬 **论坛交流**：发布帖子、编辑、删除、富文本编辑
- 🏆 **排行榜**：热门电影、评分排行
- 👤 **个人中心**：个人信息管理、收藏列表
- 📢 **公告查看**：系统公告展示
- 🔐 **用户认证**：注册、登录、登出

### 管理后台功能

- 📊 **数据统计**：电影、用户、分类、公告统计图表
- 🎥 **电影管理**：添加、编辑、删除电影，支持搜索和分页
- 📁 **分类管理**：分类的增删改查
- 👥 **用户管理**：查看、编辑、删除用户信息
- 📢 **公告管理**：发布、编辑、删除公告
- ⚙️ **系统设置**：操作日志查看
- 🔒 **权限控制**：管理员登录验证

## 安装和运行

### 环境要求

- Node.js >= 16.0.0
- MySQL >= 8.0
- npm 或 yarn

### 1. 克隆项目

```bash
git clone <repository-url>
cd dianying
```

### 2. 安装依赖

```bash
npm install
```

### 3. 配置数据库

编辑 `server/db-mysql.cjs`，修改数据库连接信息：

```javascript
const pool = mysql.createPool({
  host: "localhost", // 数据库地址
  port: 3306, // 端口
  user: "root", // 用户名
  password: "your_password", // 密码
  database: "your_database", // 数据库名
  // ...其他配置
});
```

### 4. 初始化数据库

```bash
# 清空数据库（可选）
npm run server:clear

# 初始化数据库表和初始数据
npm run server:init

# 或者一键重置（清空 + 初始化）
npm run server:reset
```

### 5. 启动项目

**启动后端服务器（端口 3000）：**

```bash
npm run server
```

**启动前端开发服务器（端口 5173）：**

```bash
npm run dev
```

### 6. 访问项目

- **前台用户端：** http://localhost:5173
- **后台管理端：** http://localhost:5173/backend
- **后端 API：** http://localhost:3000

## 数据库结构

### 主要数据表

#### movies（电影表）

- `id` - 电影 ID（主键）
- `name` - 电影名称
- `score` - 评分
- `image` - 封面图片
- `description` - 描述
- `release_date` - 上映日期
- `genre` - 类型（JSON 数组）
- `director` - 导演
- `actors` - 演员（JSON 数组）
- `tags` - 标签（JSON 数组）
- `video_url` - 视频链接
- `category_id` - 分类 ID（外键）

#### users（用户表）

- `id` - 用户 ID（主键）
- `account` - 账号
- `password` - 密码
- `nickname` - 昵称
- `avatar` - 头像
- `gender` - 性别
- `age` - 年龄
- `signature` - 个性签名
- `hobbies` - 爱好（JSON 数组）
- `favorites` - 收藏（JSON 数组）
- `comments` - 评论（JSON 数组）
- `identity` - 身份（普通用户/管理员）

#### categories（分类表）

- `id` - 分类 ID（主键）
- `name` - 分类名称
- `description` - 描述

#### announcements（公告表）

- `id` - 公告 ID（主键）
- `content` - 内容
- `created_at` - 创建时间

#### posts（帖子表）

- `id` - 帖子 ID（主键）
- `content` - 内容
- `author` - 作者
- `time` - 发布时间

## API 接口文档

### 基础 URL

```
http://localhost:3000
```

### 主要接口

#### 电影相关

- `GET /movies` - 获取电影列表（支持分页）
- `GET /movies/:id` - 获取电影详情
- `POST /movies` - 创建电影
- `PUT /movies/:id` - 更新电影
- `DELETE /movies/:id` - 删除电影

#### 用户相关

- `GET /users` - 获取用户列表
- `GET /users/:id` - 获取用户详情
- `POST /users` - 创建用户（注册）
- `PUT /users/:id` - 更新用户信息
- `DELETE /users/:id` - 删除用户
- `GET /users/:id/favorites` - 获取用户收藏
- `POST /users/:id/favorites` - 添加收藏
- `DELETE /users/:id/favorites/:movieId` - 取消收藏

#### 分类相关

- `GET /categories` - 获取分类列表
- `POST /categories` - 创建分类
- `PUT /categories/:id` - 更新分类
- `DELETE /categories/:id` - 删除分类

#### 公告相关

- `GET /announcements` - 获取公告列表
- `POST /announcements` - 创建公告
- `PUT /announcements/:id` - 更新公告
- `DELETE /announcements/:id` - 删除公告

#### 帖子相关

- `GET /posts` - 获取帖子列表
- `POST /posts` - 创建帖子
- `PUT /posts/:id` - 更新帖子
- `DELETE /posts/:id` - 删除帖子

## 开发说明

### 前后端分离架构

- **前端**：使用 Vite 开发服务器，支持热更新
- **后端**：Express 服务器提供 RESTful API
- **通信**：前端通过 Axios 调用后端 API，支持跨域（CORS）
- **数据格式**：统一使用 JSON 格式

### 文件命名规范

- **前端组件**：PascalCase（如 `HomePage.vue`）
- **后端文件**：kebab-case + .cjs 后缀（如 `movies-mysql.cjs`）
- **路由文件**：小写 + .js（如 `router.js`）
- **Store 文件**：小驼峰 + .js（如 `movieStore.js`）

### 状态管理

使用 Pinia 进行状态管理，每个功能模块独立 Store：

- 用户认证：`auth.js`
- 电影数据：`movieStore.js`
- 分类管理：`category.js`
- 用户管理：`users.js`

### 数据持久化

- **Token**：存储在 localStorage
- **操作日志**：存储在 localStorage（最多 100 条）
- **用户数据**：存储在 MySQL 数据库

## 部署说明

### 生产环境构建

```bash
# 构建前端
npm run build

# 构建产物在 dist/ 目录
```

### 部署步骤

1. 构建前端静态文件
2. 配置生产环境数据库
3. 启动后端服务器
4. 使用 Nginx 代理前端静态文件和后端 API

## 常见问题

### 1. 数据不实时更新？

确保在 CRUD 操作后调用了相应的 fetch 方法刷新数据。

### 2. 跨域问题？

后端已配置 CORS，确保前端 API baseURL 正确指向 `http://localhost:3000`。

### 3. 数据库连接失败？

检查 `server/db-mysql.cjs` 中的数据库配置是否正确。

### 4. 富文本编辑器输入字符消失？

已添加 `translate="no"` 属性禁用浏览器自动翻译。

## 许可证

MIT License

## 作者

电影管理平台开发团队
